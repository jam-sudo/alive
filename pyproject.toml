[project]
name = "alive"
version = "0.0.1"
requires-python = ">=3.11,<3.13"
dependencies = [
    "numpy",
    "scipy",
    "scikit-learn",
    "anndata",
    "scanpy",
    "h5py",
    "pyyaml",
    "pytest",
    "ruff",
]

[project.scripts]
alive = "alive.cli:main"

[project.optional-dependencies]
features = [
    "torch",
    "transformers",
    "fair-esm",
]

[tool.ruff]
line-length = 100
extend-exclude = [
    # Immutable as-run evidence: preserved byte-for-byte for provenance and never
    # imported or executed by the maintained codebase. See the archive README and
    # the 2026-07-11 probe-rerun runbook.
    "docs/superpowers/evidence/2026-07-11-gears-decision-probe/harness",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "S101"]
ignore = []

[tool.ruff.lint.per-file-ignores]
# 테스트와 스크립트의 `assert` 는 이 저장소의 검증 언어 자체다(kill 인증도 그 위에 선다).
# S101 은 production source 에만 적용한다 — `-O` 실행에서 조용히 사라지는 검사를 막는 것이 목적.
"tests/**" = ["S101"]
"scripts/**" = ["S101"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "repo_history: needs real Git history (reads blobs at recorded commits); fails in a shallow clone or a .git-less export, and is deselected from the run that produces the kernel-isolation CI receipt",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/alive"]
